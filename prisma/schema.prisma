generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  userId BigInt @id @unique
  lives Int @default(3)
  liveTimestamp DateTime? @db.Timestamp()
  score BigInt @default(0)
}